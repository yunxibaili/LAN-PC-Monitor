# v5.0 依赖清单（见《README.md》§16.1）
# ===================== 拆分安装指南 =====================
# 共用依赖（Agent + Host 都要装）：
#     pip install -r requirements-common.txt
#
# Agent（服务端）额外依赖：
#     pip install -r requirements-agent.txt
#
# Host（前端）额外依赖：
#     pip install -r requirements-host.txt
#
# 一键安装全部（开发/调试用）：
#     pip install -r requirements-agent.txt -r requirements-host.txt
# ===================== 共用（Agent + Host）=====================
psutil>=5.9.0
py-cpuinfo>=9.0.0
nvidia-ml-py>=11.5.0   # GPU（import pynvml；NVML 随 NVIDIA 驱动自带，无需 CUDA）
wmi>=1.5.1
pywin32>=305
numpy>=1.24.0           # 帧差分计算（dxcam 依赖）

# 帧率降级（DXGI 截帧；仅 Windows）
dxcam>=0.0.5

# 网络（psutil.net_if_addrs 选 IP + mDNS 零配置发现）
zeroconf>=0.132.0      # mDNS/Bonjour 自动发现；未安装时自动降级仅 UDP 广播

# 可选（AMD GPU）
pyadl>=0.1

# ===================== Agent 必需（服务端）=====================
websockets>=12.0        # Agent WebSocket 服务端
aiohttp>=3.9.0          # Agent HTTP/REST 服务端（可与 WS 共用端口）

# ===================== Host 必需（前端）=====================
PyQt5>=5.15.0           # Host GUI（原生桌面，见《README.md》§6.2）
websocket-client>=1.7   # Host WS/REST 客户端（与 aiohttp 二选一）
requests>=2.31.0        # Host REST API 客户端（轻量）
