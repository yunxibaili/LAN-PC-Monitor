# RC-5 环境说明（Known Environment Issue）

> **生成**: 2026-08-13
> **目的**: 记录基线冻结时的已知环境问题，避免后续 AI 误判为「测试失败」。

---

## Known Environment Issue

| 项 | 值 |
|----|-----|
| 受影响测试 | `tests/test_v52_phase0.py`、`tests/test_v52_phase2.py` |
| 失败现象 | 进程以返回码 `3221225477`（`0xC0000005`，Windows 原生访问冲突）崩溃，无任何测试输出 |
| 范围 | 环境依赖（原生库访问冲突，疑与硬件采集/NVML/ctypes 相关） |
| 影响 | **不影响应用架构验证**；这两个套件不在 GUI/Theme/架构一致性断言范围内 |

---

## 已确认通过的核心测试

```
test_v52_* 其余 21 套件:  664 通过, 0 失败
test_api:                 14 通过, 0 失败
test_p0:                  45 通过, 0 失败
──────────────────────────────────────
合计:                     723 通过, 0 失败
```

---

## 判定

- `test_v52_phase0` / `test_v52_phase2` 的崩溃**不阻塞 RC-5 基线冻结**。
- 后续 AI 在环境中遇到这两个套件崩溃时，应视为**已知环境问题**，而非新引入的回归。
- 若需排查，优先检查运行环境的原生依赖（GPU 采集 NVML / LibreHardwareMonitor / ctypes 调用）。
